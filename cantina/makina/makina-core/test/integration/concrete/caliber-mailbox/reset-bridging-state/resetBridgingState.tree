resetBridgingState.t.sol
├── when caller is not the Security Council
│  └── it should revert with UnauthorizedCaller
└── when caller is the Security Council
   ├── given the token is not a registered base token in associated caliber
   │  └── it should revert with NotBaseToken
   └── given the token is a registered base token in associated caliber
      ├── it should erase all the existing bridge counters for the given token, in caliber mailbox storage
      ├── it should withdraw token balance in all bridge adapters to caliber, and wipe out pending transfers
      └── it should emit a BridgingStateReset event
